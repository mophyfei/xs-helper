# LastDayOfMonth -  (系統函數)（日期相關）

## 語法

取得指定月份的天數。  
回傳數值=LastDayOfMonth(月份)  
傳入一個參數:  
- 第一個參數是月份，1為1月、2為2月...依此類推。

## 說明

傳回指定月份的天數。

例如：一月有31天、二月只有28天、四月有30天。

範例：

```xs
value1 = LastDayOfMonth(month(date)); //取得當月的天數
```
