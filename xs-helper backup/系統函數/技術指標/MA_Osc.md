# MA_Osc -  (系統函數)（技術指標）

## 語法

計算移動平均線擺盪指標。  
回傳數值=MA\_Osc(短期數,長期數)  
傳入二個參數:  
- 第一個參數是計算快速線（短期）的期數。  
- 第二個參數是計算慢速線（長期）的期數。

## 說明

MA\_Osc是計算二條不同期數的簡單移動平均線差值而得。

範例：

```xs
value1 = MA_Osc(5,10);       //計算MA-Osc
plot1(value1, "MA_Osc");
```
