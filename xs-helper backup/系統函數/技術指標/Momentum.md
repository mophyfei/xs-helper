# Momentum -  (系統函數)（技術指標）

## 語法

計算數列指定期間的變動量。  
回傳數值=Momentum(數列,期數)  
傳入二個參數:  
- 第一個參數是數列，通常是開高低收的價格數列。  
- 第二個參數是期數。

## 說明

計算目前K棒與N根K棒之前的差值。

計算公式：

Momentum = 當期價格 - 前N期價格

範例：

```xs
value1 = Momentum(Close,10);       //計算收盤價10期的運動量指標
plot1(value1, "MTM");
```
