# LowDays -  (系統函數)（價格關係）

## 語法

計算指定期間內創新低的次數。  
回傳數值=LowDays(期數)  
傳入一個參數:  
- 第一個參數是期數。期數含當期，最小值為1。

## 說明

範例：

```xs
plot1(LowDays(10)); //繪製近10期創新低次數的連線
```

請參考[HighDays函數](api?a=highdays&b=sys)。
