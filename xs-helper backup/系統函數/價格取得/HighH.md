# HighH -  (系統函數)（價格取得）

## 語法

取得半年線的最高價。  
僅限使用於半年線以下之頻率。  
回傳數值=HighH(期別)  
傳入一個參數:  
- 第一個參數是期別，和序列引用定義相同，0表當期、1表前一期...依此類推。

## 說明

當使用頻率小於半年線時，用HighH可以找到某期的半年最高價。

範例：

```xs
plot1(HighH(0)); //繪製當期半年線最高價的連線
```
