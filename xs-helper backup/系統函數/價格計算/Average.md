# Average -  (系統函數)（價格計算）

## 語法

計算序列資料的移動平均。  
回傳數值=Average(數列,期數)  
傳入二個參數:  
- 第一個參數是數列，通常是開高低收的價格數列。  
- 第二個參數是期數。

## 說明

計算序列資料的移動平均。

以最新一筆資料為基準點，輸入要計算的期數，然後計算過去期數的序列平均值。

範例：

```xs
value1 = Average(Close,5); //計算5期收盤價的移動平均
```
