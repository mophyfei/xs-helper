# 最低(日) (報價欄位)

## 語法

```xs
Value1 = GetQuote("最低(日)");
Value1 = GetQuote("DailyLow");
Value1 = q_DailyLow;
```

| 屬性 | 值 |
|---|---|
| 欄位分類 | 常用 |
| 單位 | 元 |
| 格式 | 數值 |
| 支援腳本 | 警示 交易 函數 |
| 支援商品 | 全部 |

## 說明

當日的最低價。也可以使用 GetField("Low", "D") 來表示。
