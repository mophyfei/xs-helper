# IntPortion -  (內建函數)（數學函數）

## 語法

計算傳入數值的整數部分的數值  
回傳數值 = IntPortion(數值)

## 說明

範例:

```xs
Value1 = IntPortion(10.5);   // Value1 = 10
Value2 = FracPortion(10.5);  // Value2 = 0.5
```

請參考 [FracPortion函數](api?a=fracportion&b=bif)。
