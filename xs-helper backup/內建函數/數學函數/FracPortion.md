# FracPortion -  (內建函數)（數學函數）

## 語法

計算數值的小數部分  
回傳數值 = FracPortion(數值)

## 說明

範例:

```xs
Value1 = IntPortion(10.5);   // Value1 = 10
Value2 = FracPortion(10.5);  // Value2 = 0.5
```

請參考 [IntPortion函數](api?a=intportion&b=bif)。
