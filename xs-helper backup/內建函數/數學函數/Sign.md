# Sign -  (內建函數)（數學函數）

## 語法

回傳數值的正負號  
回傳數值 = Sign(數值)  
如果是正數，回傳1，如果是負數，回傳-1，如果是0，則回傳0

## 說明

範例:

```xs
Value1 = Sign(10);  // Value1 = 1
Value2 = Sign(-10); // Value2 = -1
Value3 = Sign(0);   // Value3 = 0
```
