# UpperStr -  (內建函數)（字串函數）

## 語法

把字串的每個英文字元轉成大寫  
回傳字串 = UpperStr(原始字串)

## 說明

範例如下:

```xs
Var: str1("");


str1 = UpperStr("abcdefg");  // str1 = "ABCDEFG"
```

請參考[LowerStr函數](api?a=lowerstr&b=bif)
