# GetFieldPublishDate -  (內建函數)（欄位函數）

## 語法

用來取得指定欄位在XQ中更新的日期。  
  
GetFieldPublishDate(欄位名稱, 頻率);  
第一個參數是要查詢的欄位名稱。  
第二個參數是要查詢的欄位頻率，預設值為執行頻率。  
回傳值為YYYYMMDD的8碼數字。

## 說明

此函數能夠取得指定的欄位在XQ中更新的日期，亦可以透過 [N] 的方式來取得該欄位前 N 期的更新日。

例如 GetFieldPublishDate("月營收", "M")[1] 回傳的是上個月月營收的公布日 (也就是 GetField("月營收", "M")[1] 的公佈日)。
